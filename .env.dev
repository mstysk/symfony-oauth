
###> symfony/framework-bundle ###
# Replace with a unique 32-char value before running locally:
#   docker compose run --rm app php -r "echo bin2hex(random_bytes(16));"
APP_SECRET=changeme-development-only
###< symfony/framework-bundle ###
